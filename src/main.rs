mod variables;
mod constants;
mod shadowing;
mod data_types;
fn main() {
    //cargo check for compiling
    //cargo run for compiling and excuting
    //cargo init for boostrapping a new project
    //We can build a project using cargo build
    //cargo new "project name" for creating a named project and also boostrapping the project
    //cargo build --release to compile it with optimizations.
    println!("Hello, world!");
}
